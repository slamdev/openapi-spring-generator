openapi: "3.0.0"
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /test:
    get:
      operationId: test
      responses:
        '200':
          description: test
          content:
            application/json:
              schema:
                type: string
