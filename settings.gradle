pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url 'https://oss.sonatype.org/content/repositories/snapshots'
        }
    }
}

rootProject.name = 'openapi-spring-generator'

include 'generator', 'plugin', 'showcase:client', 'showcase:server'
